apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: sabnzbd
  namespace: media
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: sabnzbd
  endpoints:
    - port: metrics
      scheme: http
      path: /metrics
      interval: 1m
      scrapeTimeout: 10s
