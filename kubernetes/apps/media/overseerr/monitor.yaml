apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: overseerr
  namespace: media
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: overseerr
  endpoints:
    - port: metrics
      scheme: http
      path: /metrics
      interval: 1m
      scrapeTimeout: 10s
