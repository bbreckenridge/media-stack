apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: readarr
  namespace: media
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: readarr
  endpoints:
    - port: metrics
      scheme: http
      path: /metrics
      interval: 1m
      scrapeTimeout: 10s
